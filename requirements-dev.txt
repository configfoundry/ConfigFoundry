# ConfigFoundry — development / test dependencies (pinned, exact versions)
#
# These are NOT needed to run the application and are intentionally kept
# out of requirements.txt / vendor/python/ -- an air-gapped production
# deployment never needs pytest. Installed separately (also from an
# offline wheelhouse, vendor/python-dev/, if you want `make test` to work
# air-gapped too -- see docs/airgap.md).

pytest==9.1.1

# --- Transitive dependencies, pinned explicitly ---
iniconfig==2.3.0   # transitive: pytest
packaging==26.2    # transitive: pytest
pluggy==1.6.0       # transitive: pytest
Pygments==2.20.0    # transitive: pytest
tomli==2.4.1; python_version < "3.11"          # transitive: pytest
exceptiongroup==1.3.1; python_version < "3.11" # transitive: pytest
typing_extensions==4.16.0                       # transitive: pytest
