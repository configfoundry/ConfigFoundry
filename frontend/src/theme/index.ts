// Deprecated, unused. This was the from-scratch hand-built MUI theme (Vuexy
// colors re-typed as raw values) from the "recreate Vuexy visually" pass.
// Replaced by the ACTUAL Vuexy theme system ported into @core/theme/* (real
// palette/typography/shadows/overrides files from the licensed bundle) and
// wired up in providers/ThemeModeProvider.tsx via @core/theme/ThemeOptions.ts.
// No remaining imports reference this file (verified by grep before this
// stub was written). This sandbox could not delete files from the connected
// project folder (the mount rejects unlink) -- please delete this file by
// hand.
export {}
