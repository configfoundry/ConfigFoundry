// Deprecated, unused. Moved to src/components/common/StatCard.tsx (generic,
// reusable stat tile -- not dashboard-specific) to match the modules/<name>
// + components/{common,forms,tables,charts,navigation,feedback} architecture.
// This sandbox could not delete files from the connected project folder (the
// mount rejects unlink) -- please delete this file (and the now-empty
// src/components/dashboard/ directory) by hand. Nothing imports from here
// anymore; left with no exports so a stray import fails loudly.
export {}
