// Deprecated, unused. This was the hand-built account dropdown (own Avatar +
// Menu, wrapping useAuth().logout()) from the "recreate Vuexy visually" pass.
// Replaced by Vuexy's ACTUAL UserDropdown, ported into
// @core/layouts/components/shared-components/UserDropdown.tsx (adapted to call
// ConfigFoundry's real useAuth()/logout(), demo menu items trimmed to routes
// that actually exist).
// No remaining imports reference this file (verified by grep before this
// stub was written). This sandbox could not delete files from the connected
// project folder (the mount rejects unlink) -- please delete this file by
// hand.
export {}
